[package]
name = "kairos-domain"
version = "0.1.0"
edition = "2021"
description = "Domain layer (hexagon core) for Kairos Alloy."
license = "LicenseRef-Proprietary"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

