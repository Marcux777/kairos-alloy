# Docker (avoid root-owned files on bind mounts)
# Set these to match your host user before building/running the dev container.
KAIROS_UID=1000
KAIROS_GID=1000
KAIROS_USER=kairos

# PostgreSQL (dev/CI only; not a production secret)
KAIROS_DB_USER=kairos
KAIROS_DB_PASSWORD=CHANGE_ME
KAIROS_DB_NAME=kairos
